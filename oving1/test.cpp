#include <iostream>
#include <mutex>
#include <list>
#include <thread>

using namespace std;
mutex m;

int main() {
    // Test locking
    mutex m;
    //std::unique_lock<std::mutex> lock(m);
    return 0;
}